{
  "name": "elnora-agent-dashboard-server",
  "version": "0.1.0",
  "description": "Competitive intelligence + negative-data signal dashboard for Elnora AI. Uses Claude (with web search) to research competitors and to classify/extract negative-data findings from Europe PMC.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  }
}
